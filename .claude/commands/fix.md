Your goal is to repair this project until the command `/audit` can run successfully.

You are authorized to make all necessary changes automatically without asking for confirmation.

Steps:
1. Inspect the project structure, package files, lockfiles, configs, scripts, and dependency versions.
2. Fix anything that prevents the project from installing, building, running, or being audited.
3. You may edit, replace, delete, upgrade, downgrade, or regenerate files as needed.
4. Run the required commands automatically, including install, build, lint, test, audit, and audit fix commands when available.
5. If one fix creates another error, continue iterating until `/audit` runs successfully.
6. Prefer the solution that preserves the existing architecture and minimizes unnecessary changes.
7. Log every important change and explain why it was required.

Do not stop at the first error. Keep repairing the project until it is operational and `/audit` can execute successfully.